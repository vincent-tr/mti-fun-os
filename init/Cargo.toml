[package]
name = "init"
version = "0.1.0"
edition = "2021"

[dependencies]
syscalls = { path = "../syscalls" }
libsyscalls = { path = "../libsyscalls" }
log = "0.4.20"
lazy_static = { version = "1.4.0", features = ["spin_no_std"] }
